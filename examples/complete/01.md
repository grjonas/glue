```
map : (a -> b) -> [a] -> [b]

map = fn (f, l)
	match l
	| Cons(x, xs): x :: map(f, xs)
	| Nil: Nil
	end

let hello : String =
	"Hello, World!"
    	:unpack()
    	:pack()

type RPS
| Rock
| Paper
| Scissors
end

type Victor
| POne
| PTwo
| Draw
end

fn rps(a, b)
do
	return match a
	| Rock    :
		match b
		| Rock    : Draw
		| Paper   : PTwo
		| Scissors: POne
		end
	| Paper   :
		match b
		| Rock    : POne
		| Paper   : Draw
		| Scissors: PTwo
		end
	| Scissors:
		match b
		| Rock    : PTwo
		| Paper   : POne
		| Scissors: Draw
		end
	end
end

fn play(player_a, player_b)
do
	let move_a
	let move_b

	print("Enter move for player one \"" ++ player_a ++ "\"")
	move_a = get_move()
	print("Enter move for player two \"" ++ player_b++ "\"")
	move_b = get_move()

	let msg =
		match rps(move_a, move_b)
    	| POne: "Player One won!"
    	| PTwo: "Player Two won!"
    	| Draw: "Draw!"
    	end

    msg:print()
end

```
