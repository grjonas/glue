# Scanner
- [X]: Currently, the scanner doesn't parse identifiers and keywords.
- [ ]: Also, the scanner is built almost exactly like it is in Crafting Interpreters,
    which doesn't work well for our architecture, since we also count the columns in each token.
    We'll need to rewrite the scanner later.
- [ ]: There are issues with the column counting in the scanner, fix that later.
